## 1. Task B — База даних та API

- [x] 1.1 Додати колонку `is_archived INTEGER NOT NULL DEFAULT 0` та міграцію в `app/src/db.js`, перевірити схему
- [x] 1.2 Оновити `app/src/app.js`: фільтрація активних/архівних нотаток у `GET /api/notes` та реалізація `PATCH /api/notes/:id/archive` із серверною валідацією типу
- [x] 1.3 Додати автотести на архівацію, розархівацію та валідацію в `app/test/api.test.js`, перевірити `npm test`

## 2. Task C — Авторизація та усунення засіяної вразливості

- [x] 2.1 Відтворити витік чужої нотатки в `GET /api/notes/:id` через червоний тест у `app/test/api.test.js`
- [x] 2.2 Полагодити `GET /api/notes/:id` умовою `WHERE id = ? AND user_id = ?` та прибрати витік внутрішнього поля `user_id`
- [x] 2.3 Забезпечити перевірку власника для `PATCH /api/notes/:id/archive` і переконатися, що `npm test` зелений

## 3. Task A — Інтерфейс користувача та доступність

- [x] 3.1 Додати доступні вкладки «Активні» / «Архів» та розділені порожні стани у `app/public/index.html`
- [x] 3.2 Реалізувати динамічне перемикання фільтра, виклики `PATCH /api/notes/:id/archive` та відновлення у `app/public/app.js`
- [x] 3.3 Стилізувати кнопки дій, мінімальний розмір цілі (40px) та контури фокусу `:focus-visible` у `app/public/style.css`
- [x] 3.4 Запустити застосунок через `npm run dev` та перевірити роботу в браузері

## 4. Task D & Task E — Документація та бонуси

- [x] 4.1 Заповнити `docs/ai-mistakes.md` (3 конкретні кейси помилок AI з доказами та гіпотезами)
- [x] 4.2 Заповнити `docs/task-e-bonus.md` (Шлях 3: матриця авторизації всіх маршрутів під чужим користувачем)
